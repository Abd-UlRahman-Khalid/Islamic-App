import 'package:flutter/material.dart';

class SabhaTab extends StatelessWidget {
  const SabhaTab({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      color: Colors.green,
    );
  }
}
